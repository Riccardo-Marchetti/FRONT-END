import { Spinner } from "react-bootstrap";

// Loading component displaying a spinner while content is loading
const Loading = () => (
  <Spinner animation="border" variant="success" className="mt-2" />
);

export default Loading;
